"""Pure, I/O-free math over frames and dataclasses.

Nothing under ``sobres.core`` performs network, disk, database, logging or
tracing I/O — ``tests/architecture/`` enforces it by import scanning.
"""
